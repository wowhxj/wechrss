# 安全策略

## 支持范围

安全修复优先合入最新主版本。旧版本可能只提供升级建议。

## 报告安全问题

请不要通过公开 Issue 披露未修复的漏洞。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能，并提供：

- 受影响版本；
- 可复现步骤或最小示例；
- 潜在影响；
- 已知缓解方式（如有）。

请勿在报告中附带真实微信读书 Token、RefreshToken、VID、二维码或包含个人内容的数据库。维护者确认问题后会协调修复与披露时间。

## 安全边界

WeRSS 是单用户、自托管工具，不提供多租户权限模型。默认仅监听本机；远程暴露管理端时，部署者需要配置 `ADMIN_PASSWORD`、HTTPS 反向代理和适当的网络访问控制。

项目不会接受以绕过验证码、人工验证、限频或平台风控为目的的功能。
